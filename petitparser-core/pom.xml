<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.petitparser</groupId>
        <artifactId>petitparser</artifactId>
        <version>2.4.2</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>petitparser-core</artifactId>
    <name>PetitParser Core</name>
    <description>PetitParser library</description>

    <build>
        <plugins>
            <!--
              The allocation baseline test reads the JVM thread-allocation
              counters through reflection; open the management modules to the
              test run only (the production module descriptor is unchanged).
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>
                        --add-reads petitparser.core=java.management
                        --add-reads petitparser.core=jdk.management
                        --add-opens jdk.management/com.sun.management.internal=petitparser.core
                    </argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
